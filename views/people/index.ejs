<!doctype html>
<html>
  <head>
  </head>
  <body>
    <h1> List of People </h1>

    <% people.forEach(function (allPeople) { %>
    <div>
      <p><a href="/people/<%=allPeople.id%>"> <%= allPeople.firstname %> <%= allPeople.lastname %> </a></p>
    </div>

    <% }) %>
    
 

  </body>
</html>